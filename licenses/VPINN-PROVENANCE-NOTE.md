# VPINN Provenance Note

Local path:

- `pinnacle/vpinn/`

Status:

- provenance partially reconstructed

What is known:

- the subtree was imported into this repository as part of the upstream PINNacle benchmark snapshot,
- the code implements VPINN / hp-VPINN-style methods,
- a closely related public reference implementation is:
  https://github.com/ehsankharazmi/hp-VPINNs
- that related public repository declares an MIT license.

What is not yet fully reconstructed:

- the exact public repository and commit used for every file in `pinnacle/vpinn/`,
- whether all files descend directly from a single upstream source,
- the exact original license headers for each imported file.

Additional file-level provenance already present:

- `pinnacle/vpinn/vpinn/pdebench_err.py` states that it was modified from PDEBench.

Repository-local note:

- until a full file-by-file provenance pass is completed, treat `pinnacle/vpinn/` as an experimental subtree with best-effort attribution.
