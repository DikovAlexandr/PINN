# FBPINNs License Note

Local path:

- `pinnacle/fbpinns/`

Upstream project:

- Repository: https://github.com/benmoseley/FBPINNs

Declared upstream license:

- MIT

Verification notes:

- the public upstream repository declares `MIT license`,
- the repository exposes a root `LICENSE` file alongside `README.md`.

Repository-local note:

- the current `pinnacle/fbpinns/` subtree is a vendored and locally reorganized copy used for benchmark integration,
- the imported local snapshot no longer contains the original upstream `LICENSE` file,
- this note preserves the upstream provenance until a verbatim copy of the original upstream license file is restored locally.
